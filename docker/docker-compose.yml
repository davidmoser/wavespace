version: '3.8'

services:
  dependencies:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    image: gebregl/wavespace-dependencies:v1

  spectrogram_autoencoder:
    build:
      context: ..
      dockerfile: docker/spectrogram_autoencoder/Dockerfile
    image: gebregl/wavespace-spectrogram-autoencoder:v1
    volumes:
      - /home/david/mine/projects/wavespace/resources:/resources
